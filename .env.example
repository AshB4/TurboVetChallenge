# Authentication
JWT_SECRET=change-me
JWT_EXPIRES_IN=3600

# Database
# Supported values: sqlite (default) or postgres
DB_TYPE=sqlite
# For sqlite this can point to a local file (e.g. dev.db).
# Override with a Postgres connection string when DB_TYPE=postgres.
DB_URL=dev.db
